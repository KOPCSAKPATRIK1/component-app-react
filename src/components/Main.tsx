import { Component, ReactNode } from "react";


export default class Main extends Component {
    render(): ReactNode {
        return <main>
            <p>This is Zsigmond homosexual </p>
        </main>
    }
}